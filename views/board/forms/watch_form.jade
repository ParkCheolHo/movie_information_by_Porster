//
   Created by WhiteNight on 2016-06-07

.wrapper
    .header
        h4 자유게시판
    .content-body
        .page-header
            h2 #{content.title}

        p #{content.content}

    .footer
        if(content.user == name)
            form(action='/board/delete/#{content.board_index}?_method=delete' method='post')
                button.btn.pull-right.btn-primary(href = type='button' onclick="return confirm('정말 삭제 하시겠습니까?');") 삭제
            a.btn.pull-right.btn-primary(href='/board/#{content.board_index}/edit' type='button') 수정
include ../../script